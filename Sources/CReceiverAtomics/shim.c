#include "include/CReceiverAtomics.h"

/* All entry points are static inline in the header; this translation unit
   exists only because SwiftPM requires a C target to have a source file. */
void scr_atomics_link_stub(void) {}
